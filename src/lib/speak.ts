// RoutineNest — TTS stub
// TODO: Replace with Web Speech API / Capacitor TTS plugin

/**
 * Speak the given text aloud.
 * Currently a console.log stub — will be wired to
 * Web Speech API (browser) or Capacitor TTS plugin (native).
 */
export function speak(text: string): void {
  console.log("[speak]", text);
}
